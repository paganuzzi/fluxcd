apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: route
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`netbirdapp.k3d`)
      kind: Rule
      services:
        - name: netbird-netbirdapp
          port: 80
