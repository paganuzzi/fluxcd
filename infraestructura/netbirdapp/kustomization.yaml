apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: netbird
  namespace: flux-system
spec:
  interval: 10m
  targetNamespace: netbird-system
  prune: true
  sourceRef:
    kind: GitRepository
    name: netbird-repo
  path: ./app/netbirdapp/
  timeout: 1m
